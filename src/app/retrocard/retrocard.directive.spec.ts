import { RetrocardDirective } from './retrocard.directive';

describe('RetrocardDirective', () => {
  it('should create an instance', () => {
    const directive = new RetrocardDirective();
    expect(directive).toBeTruthy();
  });
});
